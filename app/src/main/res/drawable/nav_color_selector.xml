<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Couleur quand l'item est sélectionné -->
    <item android:color="@android:color/white" android:state_checked="true"/>
    <!-- Couleur quand l'item n'est pas sélectionné -->
    <item android:color="@android:color/white" android:state_checked="false"/> <!-- Gris à 50% -->
</selector>